<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiaokui.mapper.PublicMapper">
    <select id="getClassHaveTeacher" resultType="com.xiaokui.dto.ClassOnlyIdName">
        select c.id, c.name
        from class c
                 join teacher t on c.id = t.class_id and t.is_delete = false
        where c.is_delete = false;
    </select>
    <select id="getClassNoTeacher" resultType="com.xiaokui.dto.ClassOnlyIdName">
        select c.id, c.name
        from class c
        where c.is_delete = false
          and c.id not in (select t.class_id from teacher t where t.class_id is not null and t.is_delete = false);
    </select>
</mapper>